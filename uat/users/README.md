# UAT user fixtures (reference)

Runtime seeding uses:

- **PR-phase / Desk KT UAT users:** `kentender.uat.bootstrap.seed_pr_uat_users_console`
- **MVP pack (through PR) — internal + supplier fixtures:** `kentender.uat.mvp.commands.seed_uat_mvp` (see [../README.md](../README.md))

Email patterns and roles for MVP are defined in [`../seed_packs/mvp_canonical.json`](../seed_packs/mvp_canonical.json) and the bundled copy under `kentender/kentender/uat/mvp/data/mvp_canonical.json`.
