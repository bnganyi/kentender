# KenTender project rules (Cursor)

The **authoritative** KenTender rules file for Cursor is at the bench root:

`frappe-bench/.cursor/rules/kentender.mdc`

It is scoped with `globs: apps/kentender_platform/**`, so it applies when you work on files under the KenTender monorepo (`kentender_platform/`).

Edit that `.mdc` file to change platform rules; avoid duplicating the full text here.
